package ifElseStatements.switchStatements;

public class SwitchStatements {
    public static void main(String[] args){

        /*
        Statement: it is a one line of code

        public class Test{
            public static void main(String[] args){
                System.out.println("Hello World"); // this is a statement
                System.out.println("I start coding");

                System.out.println("End of the program!");
            }
        }
        if(username.exist() && password.exist()){
            // you are logged in
        }else{
            // you are prompted an error
        }

        NOTE: Each statement must end with a ';' (semi-colon);
        We write one statement per line


        1. if statement
        SYNTAX
        if(condition){
            //this code block will be executed
        }
        NOTE: condition MUST ALWAYS be true or false
        It should be returning a boolean

        boolean isRadniOnCampusToday = false;
        if(isRadniOnCampusToday){
            System.out.println("Radni is on campus today");
        }

        2. if - else statements

        boolean isRadniOnCampusToday = false;
        if(isRadniOnCampusToday){
            System.out.println("Radni is on campus today");
        }
        else {
            System.out.println("Radni is not on campus today");
        }


        OPPORTUNITY COST

        3. if - else if - else statements
        if(isThereSchool){
            //I will be at school
        }
        else if(isThereSoccerGame){
            //I will watch
        }
        else if(isThereANiceShowOnNetflix){
            //I am down for it
        }
        else{
            //I will sleep early and get rest
        }


        4. nested if - else statements
        if(condition){
            if(condition){

            }
            else{
            }
        }
        else{

        }




        5. SHORT-CUT if else
        if (num1 > num2) System.out.println("The greatest of " + num1 + " and " + num2 + " is = " + num1);
        else System.out.println("The greatest of " + num1 + " and " + num2 + " is = " + num2);




        6. Ternary operator
        dataType variableName = condition ? option1 : option2;
        int greatestNumber = 5 < 23 ? 23 : 5;


        String name1 = "Kaycee";
        String name2 = "Kaly";

        int ageKaly = 19;
        int ageKaycee = 20;

        Ex.1
        if(ageKaly > ageKaycee){
            oldest = "Kaly";
        }
        else{
            oldest = "Kaycee";
        }
        String oldest = ageKaly < ageKaycee ? "Kaycee" : "Kaly"

        System.out.println(oldest);

        7. Switch Statements
        - It is kind of an if else statement but based on cases
        instead of conditions
        Syntax:
        Switch(expression) {
            case value:
                // code to be executed
            break; //optional
            case value:
                // code to be executed
            break; // optional
            default: // Optional
                // code to be executed
        - byte, short, int, char, String and Enum can be used in
        switch statement
        - Java allows to have any number of case statements
        within a switch
        - Each case is followed by ":" (colon)
        - The value of a case must be the same data type as the
        variable in the switch
        - we cannot have 2 or more cases with the same name
        - default is optional and if none of the cases matched,
        then default will be executed
        - break is also optional, if we don't break for matching
        case, then it will continue executing other cases as well
        until a break statement reached.
        - It will always start execution from the case that
        matches with variable, and continue till first break
        statement

        BREAK STATEMENT
        - In Java break statement is used for the termination
        - It is used with switch statements and loop statements
        Syntax:
        break;

        DEBUGGING
        - Put the Breakpoint next to line you want to debug


        1. Search skills
        2. Debug skills
        3. Approach


         */






    }
}
