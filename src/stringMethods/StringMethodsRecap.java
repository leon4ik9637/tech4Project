package stringMethods;

public class StringMethodsRecap {
    public static void main(String[] args) {
        /*
        RECAP:
        String: It is class that is used to store objects as sequence of characters
        This class provides many useful methods to manipulate the string data
        "I like Java" -> "Java" // the last word

        1. valueOf() -> converts given variable into a String
        2. concat() -> combines multiple Strings into a new one
        3. equals() -> compares two Strings (case-sensitive)
        4. equalsIgnoreCase() -> compares two Strings (case-sensitive)
        5. toUppercase() & toLowercase() -> converts given String to uppercase or lowercase
        6. charAt() -> returns a char at given index
        7. indexOf() & lastIndexOf() -> returns the index of given char or String
        8. length() -> returns the total number of characters in a String

        String name1 = "Taylor";
        char first = name1.charAt(0);
        char last = name1.charAt(name1.length()-1);
        String middle = "" + name1.charAt(length()/2-1) + name.charAt(length()/2);

        'y' + 'l' -> 108 + 121 = 229


        String name2 = "Alona";
        char first = name2.charAt(0);
        char last = name2.charAt(name2.length()-1);
        char middle = name2.charAt(name2.length()/2);

        Please print the middle character from the name
        Alona   -> o    -> 2    -> name.length()/2 -> 2
        Guluzar -> u    -> 3    -> name.length()/2 -> 3
        Ali     -> l    -> 1    -> name.length()/2 -> 1

        Taylor  -> yl   -> 2-3  -> name.length()/2-1, name.length()/2
        John    -> oh   -> 1-2  -> name.length()/2-1, name.length()/2
        Mohammed-> am   -> 3-4  -> name.length()/2-1, name.length()/2

         */
    }
}
