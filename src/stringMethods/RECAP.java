package stringMethods;

public class RECAP {
    public static void main(String[] args) {
        /*
        1. What is a method?
        Function or behaviours of the class.
        Invokes the code of block usually inside the curly brackets
        accessModifier returnType methodName() {
            methodBody
        }
        2. How do we know if its non-static or static?
        - static is className and non-static is objectName
        3. Return type specifies what type of values the method will return.

        4. Arguments act as a variable inside the method.
        methodName should be meaningful



         */
    }
}
